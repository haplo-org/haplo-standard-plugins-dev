switch(entry.data.action) {
    i("{} took the task over from {previous}")
      { <i> entry.user.name </i> } previous { <i> entry.data.previousUserName </i> }
} delegate {
    i("{} delegated the task to {current}")
        { <i> entry.user.name </i> } current { <i> M.workUnit.actionableBy.name </i> }
}
