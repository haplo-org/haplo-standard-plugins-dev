switch(entry.data.action) {
    i("{} took the task over{previous}")
      { <i> entry.user.name </i> } previous { if(previousUser) { " from " <i> previousUser.name </i>}}
} delegate {
    i("{} delegated the task{current}")
        { <i> entry.user.name </i> } current { if(newUser) { " to " <i> newUser.name </i> }}
}
