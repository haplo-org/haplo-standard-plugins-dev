std:plugin:resources("quick-impersonate.js")
std:form:token()
if(currentlyWithUid) {
<a href="#" class="quick-impersonate" style="display: none;" data-uid=currentlyWithUid>"Impersonate"</a>
}

<div>
  processName
  unless(workUnit.closed) {
    if(status) {
      <br> <b> status </b>
    } else {
      " (complete)"
    }
  }
  render(timeline)
</div>
