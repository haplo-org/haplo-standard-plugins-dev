#option:no-tag-attribute-quote-minimisation

<h1> <a href=$actionFullUrl> title </a> </h1>

if(status) { <h3> status </h3> }

each($notesDeferreds) { render(.) }

<p class="button">
    <a href=$actionFullUrl> button </a>
</p>

each($endDeferreds) { render(.) }
