#option:disable-debug-comments
url(
  "https://"
  hostname "/download/"
  file.digest "/"
  file.fileSize "/"
  if(options.transform) { options.transform "/" }
  if(options.maximiseCompatibility) { "webdisplay/" }
  file.filename
)
